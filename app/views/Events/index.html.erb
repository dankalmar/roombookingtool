<h1> Here is a list of every event. Ever. </h1>


<% @events.each do |e| %>
<%= e.title %>
<%= e.description %>
<%= e.user %>
<%= e.time %>
	<% end %>


<%= link_to 'Book an event', new_event_path, :class => "btn btn-primary" %>
