
<h1> Click on a date to check what events are scheduled for that day.</h1>
	<div class="span6">

<%= render "calendar" %>

<% @events.each do |d| %>
	<%= d.title %>
	<%= d.description %>
<% end %>

<%= link_to 'Book an event', new_event_path, :class => "btn btn-primary" %>
	</div>


