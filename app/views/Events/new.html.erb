<div class="container">
	<% if @event.errors.any? %>
		<div class='alert'>
			<% @event.errors.full_messages.each do |message| %>
				<%= message %>
			<% end %>
		</div>
	<% end %>

	<%= render 'form' %>
</div>
